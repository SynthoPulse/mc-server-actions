name: Minecraft Server

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  server:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository files
        uses: actions/checkout@v4

      - name: Install Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Restore world from cache
        uses: actions/cache/restore@v4
        with:
          path: |
            world
            world_nether
            world_the_end
            banned-players.json
            banned-ips.json
            ops.json
            whitelist.json
          key: mc-world-save
          restore-keys: |
            mc-world-

      - name: Download and install Forge
        run: |
          echo "Downloading Forge 1.19.2-43.3.5..."
          wget -q https://maven.minecraftforge.net/net/minecraftforge/forge/1.19.2-43.3.5/forge-1.19.2-43.3.5-installer.jar
          echo "Installing Forge server..."
          java -jar forge-1.19.2-43.3.5-installer.jar --installServer
          rm forge-1.19.2-43.3.5-installer.jar
          rm forge-1.19.2-43.3.5-installer.jar.log 2>/dev/null || true
          echo "eula=true" > eula.txt
          echo "Forge installed!"

      - name: Configure server
        run: |
          cat > server.properties << 'PROPS'
          server-port=25565
          gamemode=survival
          difficulty=hard
          max-players=3
          view-distance=6
          simulation-distance=4
          allow-flight=true
          pvp=false
          motd=SynthoPulse Server
          online-mode=false
          spawn-protection=0
          enable-command-block=true
          PROPS

      - name: Setup ops.json
        run: |
          cat > ops.json << 'OPS'
          [
            {
              "uuid": "00000000-0000-0000-0000-000000000001",
              "name": "MustafaX",
              "level": 4,
              "bypassesPlayerLimit": true
            },
            {
              "uuid": "00000000-0000-0000-0000-000000000002",
              "name": "muhammad12000",
              "level": 4,
              "bypassesPlayerLimit": true
            },
            {
              "uuid": "00000000-0000-0000-0000-000000000003",
              "name": "AbbasMaster",
              "level": 4,
              "bypassesPlayerLimit": true
            }
          ]
          OPS
          echo "ops.json created!"

      - name: Install and connect Playit automatically
        env:
          PLAYIT_SECRET: ${{ secrets.PLAYIT_SECRET }}
        run: |
          echo "Downloading Playit..."
          curl -L -o playit https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-linux-amd64
          chmod +x playit
          
          echo "Creating Playit config with secret key..."
          cat > playit.toml << EOF
          secret_key = "$PLAYIT_SECRET"
          EOF
          
          echo "Starting Playit..."
          PLAYIT_SECRET_KEY="$PLAYIT_SECRET" ./playit > playit.log 2>&1 &
          
          sleep 30
          
          echo "========================================="
          echo "  Playit Status:"
          cat playit.log
          echo "========================================="
          
          if grep -q "connected" playit.log 2>/dev/null; then
            echo "SUCCESS: Playit connected automatically!"
          elif grep -q "claim" playit.log 2>/dev/null; then
            CLAIM_URL=$(grep -o "https://playit.gg/claim/[a-zA-Z0-9]*" playit.log | head -1)
            echo "========================================="
            echo "  OPEN THIS URL AND PRESS CLAIM:"
            echo "  $CLAIM_URL"
            echo "========================================="
            sleep 60
          fi

      - name: Start Minecraft Server
        run: |
          echo "========================================="
          echo "  Starting Minecraft Server..."
          echo "  Forge 1.19.2-43.3.5 | RAM: 4GB"
          echo "========================================="

          java -Xmx4G -Xms4G \
            -XX:+UseG1GC \
            -XX:+ParallelRefProcEnabled \
            -XX:MaxGCPauseMillis=200 \
            -XX:+UnlockExperimentalVMOptions \
            -XX:+DisableExplicitGC \
            -XX:G1NewSizePercent=30 \
            -XX:G1MaxNewSizePercent=40 \
            -XX:G1HeapRegionSize=8M \
            -XX:G1ReservePercent=20 \
            -XX:InitiatingHeapOccupancyPercent=15 \
            -XX:G1MixedGCLiveThresholdPercent=90 \
            -XX:SurvivorRatio=32 \
            -XX:+PerfDisableSharedMem \
            -XX:MaxTenuringThreshold=1 \
            -Dlog4j2.formatMsgNoLookups=true \
            @libraries/net/minecraftforge/forge/1.19.2-43.3.5/unix_args.txt nogui &

          MC_PID=$!
          echo "Waiting for server to start..."

          TIMEOUT=600
          ELAPSED=0
          while ! grep -q "Done" logs/latest.log 2>/dev/null; do
            sleep 10
            ELAPSED=$((ELAPSED + 10))
            echo "Loading mods... ($ELAPSED seconds)"
            if [ $ELAPSED -ge $TIMEOUT ]; then
              echo "Server took too long!"
              cat logs/latest.log 2>/dev/null | tail -50
              exit 1
            fi
          done

          echo "========================================="
          echo "  SERVER IS RUNNING!"
          echo "  Go to playit.gg/account/tunnels"
          echo "  Copy your fixed server address"
          echo "========================================="

          while kill -0 $MC_PID 2>/dev/null; do
            sleep 300
            echo "Auto-saving world..."
          done

      - name: Save world
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            world
            world_nether
            world_the_end
            banned-players.json
            banned-ips.json
            ops.json
            whitelist.json
          key: mc-world-save-${{ github.run_number }}
